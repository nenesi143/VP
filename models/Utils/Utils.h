#include "../Employee/Employee.h"
#include "../Supplier/Supplier.h"
#include <vector>

void SortEmployees(vector<employee*>& employees);
void SortSuppliers(vector<supplier*>& suppliers);
void DeleteSupplier(vector<supplier*>& employees);
void DeleteEmployee(vector<employee*>& suppliers);
//void PrintAllRecordsEmp(employee* emp, sizeof(emp));
//void PrintAllRecordsSup(supplier* sup, sizeof(sup));